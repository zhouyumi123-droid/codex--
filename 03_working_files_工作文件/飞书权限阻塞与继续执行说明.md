# 飞书 Base 搭建阻塞与继续执行说明

## 当前要创建的 Base

`刘校长6月直播运营工作台_A-Level_IG_DSE`

## 已完成的本地准备

已生成可直接执行的搭建规格与脚本：

- `feishu_live_base_spec.json`
- `create_feishu_live_base.mjs`

脚本计划创建：

- 8 张表：直播排期表、选题库、脚本生产表、物料资产表、直播执行表、评论问题库、线索跟进表、直播复盘表。
- 每张表的字段和基础视图。
- A-Level、IG、DSE 三门课程的首批 6 月直播排期、选题、脚本任务和物料任务。

## 当前阻塞

`lark-cli auth scopes` 显示应用已列出 Base 权限，但实际调用 `base +base-create` 时，飞书 OpenAPI 返回：

```text
App scope not enabled: required scope base:app:create [99991672]
```

飞书返回的权限开通链接：

```text
https://open.feishu.cn/page/scope-apply?clientID=cli_a9763cda117a1bd7&scopes=base%3Aapp%3Acreate
```

需要在飞书开发者后台确认：

1. `base:app:create` 已申请。
2. 权限变更已发布/生效到当前应用。
3. 如果后台提示需要重新发布应用版本，需要完成发布。

## 权限生效后的继续命令

在项目根目录运行：

```powershell
& 'C:\Program Files\nodejs\node.exe' '.\03_working_files_工作文件\create_feishu_live_base.mjs'
```

默认用 bot 身份。如果 user 身份授权恢复正常，可运行：

```powershell
& 'C:\Program Files\nodejs\node.exe' '.\03_working_files_工作文件\create_feishu_live_base.mjs' --as-user
```

